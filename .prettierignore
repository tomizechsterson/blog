# Dependencies and package manager
node_modules/
.yarn/

# Build outputs
.next/
out/
dist/
.build/

# Public static vendor assets (don’t reformat vendored or generated files)
public/static/
public/**/*.js
public/**/*.html

# Logs and coverage
coverage/
*.log

# Misc
.yarn/releases/

# Rewritten by `next build`/`next dev` on every run; letting prettier reformat it
# means the two overwrite each other and the file is permanently dirty.
next-env.d.ts
