# Dependencies and package manager
node_modules/
.yarn/

# Build outputs
.next/
out/
dist/
.build/

# Public static vendor assets (don’t reformat vendored or generated files)
public/static/
public/**/*.js
public/**/*.html

# Logs and coverage
coverage/
*.log

# Misc
.yarn/releases/

# Regenerated by Next on every run; prettier reformatting it just means the two
# rewrite each other. Untracked as of the .gitignore entry, so this is only
# belt-and-braces for a manual `prettier --write .`.
next-env.d.ts
